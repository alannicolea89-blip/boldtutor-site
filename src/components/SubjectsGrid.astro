---
type IconKey = 'book' | 'graduation' | 'history' | 'society';

interface SubjectItem {
  title: string;
  description: string;
  href?: string;
  icon?: IconKey;
}

interface Props {
  subjects: SubjectItem[];
}

const iconPaths: Record<IconKey, string> = {
  // Open book
  book: '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />',
  // Graduation / academic cap
  graduation: '<path d="M12 14l9-5-9-5-9 5 9 5z" /><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 14l9-5-9-5-9 5 9 5zm0 0l6.16-3.422a12.083 12.083 0 01.665 6.479A11.952 11.952 0 0012 20.055a11.952 11.952 0 00-6.824-2.998 12.078 12.078 0 01.665-6.479L12 14zm-4 6v-7.5l4-2.222" />',
  // Clock (time / history)
  history: '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />',
  // Group of people (society / culture)
  society: '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />',
};

const fallbackKeys: IconKey[] = ['book', 'graduation', 'history', 'society'];

const { subjects } = Astro.props;
---

<ul class="not-prose my-8 grid grid-cols-1 gap-6 sm:grid-cols-2" role="list">
  {subjects.map((subject, i) => {
    const iconKey = subject.icon ?? fallbackKeys[i % fallbackKeys.length];
    const iconPath = iconPaths[iconKey];
    return (
    <li>
      <a
        href={subject.href || '#'}
        aria-label={`Learn more about ${subject.title}`}
        class="group flex h-full cursor-pointer flex-col rounded-lg border border-border bg-background p-6 shadow-sm transition-shadow duration-200 hover:shadow-md"
      >
        <div class="mb-4 flex size-12 items-center justify-center rounded-lg bg-primary/10 text-primary transition-colors group-hover:bg-primary group-hover:text-primary-fg">
          <svg class="size-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true" set:html={iconPath} />
        </div>
        <h3 class="font-heading text-h3 font-semibold text-text">{subject.title}</h3>
        <p class="mt-2 flex-1 text-sm leading-relaxed text-text-muted">{subject.description}</p>
        <span class="mt-4 inline-flex items-center gap-1 text-sm font-semibold text-primary transition-colors group-hover:text-accent">
          Learn more
          <span class="sr-only"> about {subject.title}</span>
          <svg class="size-4 transition-transform group-hover:translate-x-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </span>
      </a>
    </li>
    );
  })}
</ul>
